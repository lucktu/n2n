libcrypto.so.0.9.8
linux32 --> lib
linux64 --> lib64

using:
insmod /lib/modules/tun.ko
edge -f -a 10.0.0.1 -c test -k test -l n2n.lucktu.com:10082

-----------------------------------------------------------

You can try the procedure below centos+debian+ubuntu
